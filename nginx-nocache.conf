# 胶片摄影助手 /camera —— 关闭缓存，保证每次打开都从服务器取最新
# 用法：把本文件放到 nginx 配置目录（如 /usr/local/nginx/conf/），
# 然后在 jikegang.com 的 server {} 块里加一行：include nginx-nocache.conf;
# 最后执行：nginx -t && nginx -s reload
location ^~ /camera/ {
    expires -1;
    add_header Cache-Control "no-cache, no-store, must-revalidate";
}
